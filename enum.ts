// Define the Direction enum
enum Direction {
    Up, 
    Down,
    Left,
    Right
  }
  
  // Specify the type of the direction variable explicitly
  let direction: Direction = Direction.Left;
  
  // Now the switch statement should work without errors
  switch (direction) {
    case Direction.Up:
      console.log('Going Up');
      break;
    case Direction.Down:
      console.log('Going Down');
      break;
    case Direction.Left:
      console.log('Going Left');
      break;
    case Direction.Right:
      console.log('Going Right');
      break;
  }
// Define the Direction enum
enum Direction {
    Up,
    Down,
    Left,
    Right
  }
  
  // Specify the type of the direction variable explicitly
  let direction: Direction = Direction.Left;
  
  // Now the switch statement should work without errors
  switch (direction) {
    case Direction.Up:
      console.log('Going Up');
      break;
    case Direction.Down:
      console.log('Going Down');
      break;
    case Direction.Left:
      console.log('Going Left');
      break;
    case Direction.Right:
      console.log('Going Right');
      break;
  }
export{}    